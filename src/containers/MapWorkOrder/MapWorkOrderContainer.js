import { connect } from 'react-redux';
import MapWorkOrderView from './MapWorkOrderView';

const mapStateToProps = (state) => {
  return {

  };
}

export default connect(null, null)(MapWorkOrderView);
