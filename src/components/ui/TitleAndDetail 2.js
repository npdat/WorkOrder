import { View, Text } from 'react-native';

const TitleAndDetail = (title, content) => {
    <View>
        <Text>{title}</Text>
        <Text>{content}</Text>
    </View>
}

export default TitleAndDetail;